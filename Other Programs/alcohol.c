#include <stdio.h>
#include <stdlib.h>

int main(int argc, char*argv[]){
	int num;
	
	printf("Enter your marks: ");
	scanf("%d", &num);
	
	if(num >= 80){
		printf("Grade: A");
	}else if(age == ){
		printf("You still have one year to take alcohol ");
	}else if(age > 20){
		printf("You are old enough, do want you want");
	}else if(age < 17){
		printf("You are still a kid. Wait");
	}else{
		printf("Please enter a valid age");
	}
	
	
	return 0;
	
	
	
	
	
}
